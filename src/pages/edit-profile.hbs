---
css:
- form-group.css
---
{{!< main}}
<main>
    <div class="block-center">
        <div class="paper">
            <h1>Редактировать профиль</h1>
            <div class="form-group">
                {{#with editprofile.inputs}}
                    {{#each this}}
                        {{> input}}
                    {{/each}}
                {{/with}}
                <div class="form-group__item">
                    <a href="profile.hbs">
                        <button class="button primary mt-4">Сохранить</button>
                    </a>
                </div>
                <div class="form-group__item">
                    <a href="profile.hbs">
                        <button class="button">Отмена</button>
                    </a>
                </div>
            </div>
        </div>
    </div>
</main>
